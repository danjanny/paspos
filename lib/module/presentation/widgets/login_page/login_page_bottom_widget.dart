import 'package:flutter/material.dart';

class LoginPageBottomWidget extends StatelessWidget {
  const LoginPageBottomWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Don't have account ?")
      ],
    );
  }
}
